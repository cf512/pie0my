generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// Define the `User` table in the database
model Pie {
  id    Int     @default(autoincrement()) @id
  name  String?
  imageUrl  String?
}